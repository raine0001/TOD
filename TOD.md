# TOD

Generated by TOD project familiarization. Update this file after major work so TOD always has current status context.

## Current Status
- Project: TOD
- Project ID: tod
- Path: E:/TOD
- Type: operations-engine
- Risk Level: critical
- Write Access: guarded
- Last Reviewed: 2026-03-18T08:20:13.7950701Z

## Git / Publish
- Git Repo: True
- Branch: main
- Remote: https://github.com/raine0001/TOD.git
- Simulation Publish Ready: True
- Simulation Discovery Passed: True
- Simulation Candidate Files: E:\TOD\scripts\Invoke-TODInquiryPauseResumeSample.ps1, E:\TOD\scripts\Start-TOD-UI-AppWindow.ps1, E:\TOD\scripts\Start-TOD-UI-Safe.ps1

## Entry Points
- scripts/TOD.ps1
- scripts/Start-TOD-UI.ps1
- ui/index.html

## Test Commands
- ./scripts/Invoke-TODTests.ps1
- ./scripts/Invoke-TODSmoke.ps1

## Boundaries
### Allowed Paths
- scripts
- tests
- ui
- docs
- tod/config
### Blocked Paths
- tod/data
- tod/state
- tod/out

## Sampled Files
- scripts/Update-TODProjectStatusFiles.ps1
- scripts/Invoke-TODGitHubProjectSimulationDaily.ps1
- scripts/Invoke-TODGitHubProjectTask.ps1
- scripts/Get-TODGitHubProjectSimulationTrend.ps1
- scripts/Register-TODGitHubProjectSimulationDailyTask.ps1
- scripts/Unregister-TODGitHubProjectSimulationDailyTask.ps1

## TOD Notes
- Use this file as the default current-status summary for TOD work in this project.
- Append work summary, commit references, and deployment notes after each significant task.
- Prefer discovery -> test -> minimal diff -> commit -> push -> verify workflow.

## TOD Activity Log
- 2026-03-18T08:20:13.8002265Z initial familiarization snapshot generated.
- 2026-03-20T00:00:00Z AgentMIM marketing docs-capture workflow completed in comm_app. Added Playwright-based auth and screenshot capture for /admin/marketing, local SQLite bootstrap for repeatable docs runtime, native CSRF exposure in the shared shell, marketing rig-preview fallback behavior for local validation, refreshed help/runbook docs, and a new screenshot set under docs/screenshots/marketing/. Clean validation succeeded on port 6011 with DISABLE_BACKGROUND_TICKS=1 and MARKETING_RIG_PREVIEW_FALLBACK_ONLY=1 when stale local processes on 6001 were serving older code.
- 2026-03-20T00:00:00Z TOD hygiene pass started. Added ignore rules for Python cache files and local RunPod archive bundles, removed tracked spokesperson `__pycache__` artifacts from git, and added a tracked MIM-facing status update at `docs/agentmim_tod_status_update.md`.
- 2026-03-20T00:00:00Z TOD hygiene pass continued. Expanded ignore coverage for generated shared_state evaluation/runtime artifacts, and removed tracked blocked-path outputs from `tod/data`, `tod/state`, `tod/out`, plus tracked conversation-eval soak reports so those local artifacts stop polluting repository status.
- 2026-03-23T00:00:00Z Added sync handoff note at `docs/tod_mim_sync_handoff_2026-03-23.md` after investigating a reported TOD/MIM divergence. Current diagnosis: TOD listener is healthy, but the canonical MIM shared export, handshake, and latest task request available at `/home/testpilot/mim/runtime/shared` all still point to objective 75, so the likely problem is upstream publication or wrong SSH target rather than local TOD ingestion logic.
- 2026-05-15T00:00:00Z Home page now reuses the existing `/get_current_position` snapshot to hydrate arm controls on load and replaces the top status slot with a live workspace simulation feed. The status panel was moved below the arm controls so the current arm view and status remain on the same page without duplicating control logic.
- 2026-05-15T00:00:00Z Home page arm hydration now uses the shared `control.js` servo render path so slider creation and last-position loading happen in one place. This avoids the earlier timing race from a separate home-only hydration script.
- 2026-05-15T00:00:00Z Home page hydration was finalized by deploying `control.js` to the live `/static` directory and cache-busting the home template. The home arm controls now load `last_position.json` on first render and show the loaded current arm position summary immediately.
- 2026-05-15T00:00:00Z Workspace clean UI now includes explicit `simulation` and `Live Sync` modes plus a `Live Sync Lock` button. Lock performs one-shot assignment from `/get_current_position` without issuing `/move` writes, while Live Sync mode mirrors slider updates to `/move`. Deployed to `192.168.1.90` under `/home/testpilot/mim_arm/templates/workspace.html` and `/home/testpilot/mim_arm/static/workspace_setup_clean.js` with cache token `v7-live-sync-lock`.
- 2026-05-15T00:00:00Z Workspace lock UX fix: `Live Sync Lock` now works even when `simulation` is selected (no mode-based disable), and the button was moved directly above the servo sliders for clarity. Lock status now echoes loaded live pose values (e.g. `[107, 70, 101, 101, 103, 0]`) so users can confirm sync immediately. Deployed to `192.168.1.90` with cache token `v8-lock-always-enabled`.
- 2026-05-15T00:00:00Z Routines reliability/speed pass: removed duplicate slider bootstrap path in `control.js` that bypassed duration logic, added frontend move queuing, changed animated moves to send staged hardware `/move` commands during the selected duration, switched safe/keyframe go-to paths to sequential servo moves, and softened serial-ready flapping to require 3 consecutive ping failures before disabling control. Deployed served script at `/home/testpilot/mim_arm/static/control.js` with routines cache token `20260515-routines-queue-speed-v8`.
- 2026-05-15T00:00:00Z Routines movement profile tuned for better feel: slow durations now use finer staged increments and slightly higher command cadence (smoother hardware motion), while fast durations keep coarser increments for responsiveness. Published via routines cache token `20260515-routines-queue-speed-v9-profile-tune`.
- 2026-05-15T00:00:00Z Added shoulder spec-aware smoothing profile using provided 12V speed data (`0.21s/60deg`) with conservative loaded-motion derate. Shoulder commands now enforce a physics-based duration floor and servo-specific command cadence bounds to reduce jerk/stall behavior under load. Published via routines cache token `20260515-routines-queue-speed-v10-shoulder-spec-tune`.
- 2026-05-15T00:00:00Z Added explicit routines UI toggle `shoulderSoftStartToggle` and persistence (`mim_shoulder_soft_start_v1` + `shoulder_soft_start` config key). Soft-start now applies a shoulder-only early-ramp curve over the first ~28% of move progress. Expanded physical profile mapping for base, elbow, wrist, hand, and claw using provided servo specs and supply constraints. Published via routines cache token `20260515-routines-queue-speed-v11-softstart-toggle-multiservo-spec`.
- 2026-05-15T00:00:00Z Hardened `/move` dispatch in routines frontend with one controlled retry on timeout/5xx/transient failures to reduce interrupted sequences under heavy command load. Published via routines cache token `20260515-routines-queue-speed-v12-softstart-retry-hardened`.
- 2026-05-15T00:00:00Z Replaced strict FIFO move queue with per-servo latest-wins coalescing plus per-servo animation generation cancellation to prevent stale movement backlog under rapid input. Added failure-streak gating so one transient move timeout no longer forces immediate serial-off state; serialReady is only dropped after repeated failures. Published via routines cache token `20260515-routines-queue-speed-v14-coalesced-failure-streak-gate`.
- 2026-05-15T00:00:00Z Added hard pending-age cutoff for queued move intents (`MAX_PENDING_MOVE_AGE_MS=2200`) so stale commands are dropped instead of executing late. Added visible routines status line (`motionQueueStatus`) that reports stale-drop events. Published via routines cache token `20260515-routines-queue-speed-v15-stale-age-cutoff-status` and home token `20260515-home-hydration-v6-stale-age-cutoff`.
- 2026-05-15T00:00:00Z Mitigated safe-position overload path by replacing full animated flood with low-load staged sends (max 4 segments per servo), single in-progress guard (`safePositionInProgress`), and reconnect+retry helper for transient move failures. Published via routines cache token `20260515-routines-queue-speed-v16-safe-lowload` and home token `20260515-home-hydration-v7-safe-lowload`.
- 2026-05-15T00:00:00Z Aligned safe-position speed with Move Duration setting by pacing low-load segments from `getMoveDurationMs()` plus servo physical duration floors. Added resilient partial-failure handling for safe-position moves (continue per-servo, inline status, reconnect trigger). Published via routines token `20260515-routines-queue-speed-v18-safe-speed-aligned` and home token `20260515-home-hydration-v9-safe-speed-aligned`.
- 2026-05-15T00:00:00Z Fixed workspace Live Sync Lock no-jump regression: lock now cancels pending live-sync timers and gates live-write scheduling while lock hydration is in progress, ensuring lock performs `/get_current_position` read + slider/sim assignment only with zero `/move` writes. Published via workspace token `20260515-clean-baseline-v9-lock-no-live-write`.
